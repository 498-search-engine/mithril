#ifndef COMMON_CLOCK_H
#define COMMON_CLOCK_H

/**
 * @brief Gets a monotonic timestamp, in seconds.
 */
long MonotonicTime();

/**
 * @brief Gets a monotonic timestamp, in milliseconds.
 */
long MonotonicTimeMs();

#endif
